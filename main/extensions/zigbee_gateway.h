#pragma once

#include "esp_err.h"

/**
 * Initialize the Virtual Zigbee Gateway.
 * (Placeholder for future hardware integration)
 */
esp_err_t zigbee_gateway_init(void);

/**
 * Start the Zigbee Gateway service.
 */
esp_err_t zigbee_gateway_start(void);
